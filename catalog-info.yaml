apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: action-service
  description: Durable lifecycle и подтверждение действий агента
  annotations:
    github.com/project-slug: portable-agent/action-service
spec:
  type: service
  lifecycle: experimental
  owner: group:default/backend
  system: portable-agent
  providesApis: [action-api]
